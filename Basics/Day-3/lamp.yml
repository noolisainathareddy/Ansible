- name: Deploy lamp stack application
  hosts: all
  gather_facts: no
  become: yes
  tasks:
    - name: Install common dependencies
      yum:
        name:
          - python3-libselinux
          - python3-libsemanage
          - firewalld
        state: installed